int w;
boolean k;

int inc(int x) {
    return x + 1;
}

boolean and(boolean a, boolean b) {
    return a && b;
}

void main() {
    int x;
    int y;
    boolean a;
    boolean b;

    x = 20;
    y = inc(x);

    while (y > 0) {
        y = y - 1;
    }

    a = (y == 0);

    if (a) {
        b = true;
    }
}
