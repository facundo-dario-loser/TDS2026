boolean random(int x, float y, boolean a) {
    x = 40;

    while (x > 0) {
        x = x - 1;
    }

    y = 256.0;

    while (y < 400.0) {
        y = y + 1.0;
    }

    if ((x == 0) && (y == 0.0)) {
        a = true;
    } else {
        a = false;
    }

    return a;
}

void main() {
    
}