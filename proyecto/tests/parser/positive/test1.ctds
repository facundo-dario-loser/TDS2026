// test metodos y variables globales

int x;
boolean y;

int dec(int x) {
    return x - 1;
}

void main() {
    int z;
    int w, x, z;
    boolean a, b, c;

    z = 40;

    while (z > 0) {
        z = dec(z);
    }

    if (z == 0) {
        y = true;
    } else {
        y = false;
    }
}
